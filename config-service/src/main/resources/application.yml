server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Gonan98/fas-examen-final.git
          search-paths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
          default-label: "master"
  security:
    user:
      name: root
      password: s3cr3t
